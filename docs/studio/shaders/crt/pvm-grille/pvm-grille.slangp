# pvm-grille — Sony PVM/BVM broadcast monitor: aperture grille, tight bloom,
# crisp scanlines, no curvature.
shaders = 1

shader0        = pvm-grille.slang
filter_linear0 = true
scale_type0    = viewport
scale0         = 1.0
wrap_mode0     = clamp_to_edge
